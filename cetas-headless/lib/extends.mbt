///|
/// Trait methods exposed for dot-syntax callers (repo convention, herdr
/// precedent) — tests drive `observer.on_event_at(...)` directly.
pub extend JsonlObserver with @posoco.Observer::{on_event_at}
