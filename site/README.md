# Cetas site

Static website and documentation for Cetas.

```bash
bun install
bun run check
bun run build
```

The production output is written to `dist/`. Documentation lives in the Starlight collection at `src/content/docs/`; the inner `docs/` directory intentionally makes the public routes start at `/docs/`.
